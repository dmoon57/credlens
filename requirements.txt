# Vercel deploy manifest ONLY — source of truth is pyproject.toml / uv.lock.
# Versions must match uv.lock exactly (checked by eye at 3.2a; scripted at 3.3).
tree_sitter==0.26.0
tree_sitter_javascript==0.25.0
tree_sitter_python==0.25.0
tree_sitter_typescript==0.23.2
