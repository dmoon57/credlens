"""credlens — precision-first, eval-backed security auditor for MCP servers.

The scan path is deterministic static analysis: no network calls, no LLM calls,
and target code is never executed.
"""

__version__ = "0.1.0"
